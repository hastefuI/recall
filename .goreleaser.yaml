version: 2

project_name: recall

builds:
  - id: recall
    binary: recall
    main: .
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}

changelog:
  disable: true
